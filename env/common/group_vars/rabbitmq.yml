rabbitmq_vhosts:
  - pulsar_nrec
  - pulsar_nmbu_orion
  - test

rabbitmq_users:
  - {name: admin,  vhost: '', 
     passwd: "{{admin_rabbitmq_password}}", tags: administrator}
  - {name: nrec_galaxy, vhost: pulsar_nrec,
     passwd: "{{nrec_rabbitmq_password}}"}
  - {name: nmbu_galaxy, vhost: pulsar_nmbu_orion,
     passwd: "{{nmbu_rabbitmq_password}}" }
  - {name: test, vhost: test, 
     passwd: "{{test_rabbitmq_password}}" }
  - {name: telegraf, vhost: '', 
     passwd: "{{telegraf_rabbitmq_password}}" }
          

